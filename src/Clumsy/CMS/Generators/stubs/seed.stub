<?php

use Faker\Factory as Faker;
use Illuminate\Database\Seeder;

class {{object_name_plural}}Seeder extends Seeder
{
	public function run()
	{
		$faker = Faker::create();

		foreach (range(1, 10) as $index) {
			{{object_name}}::create(array(

			));
		}
	}
}
